services:
  redis:
    image: docker-registry-002.zeuslearning.com/zeuslearning/redis
    container_name: trainee_redis
    restart: unless-stopped

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

volumes:
  redis_data:
